<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fjdialect.mapper.DialectResourceMapper">

    <select id="selectByCityCode" resultType="com.fjdialect.entity.DialectResource">
        SELECT * FROM dialect_resource
        WHERE city_code = #{cityCode}
        ORDER BY create_time DESC
    </select>

    <select id="selectByCategory" resultType="com.fjdialect.entity.DialectResource">
        SELECT * FROM dialect_resource
        WHERE category = #{category}
        ORDER BY create_time DESC
    </select>

    <select id="selectByDifficulty" resultType="com.fjdialect.entity.DialectResource">
        SELECT * FROM dialect_resource
        WHERE difficulty = #{difficulty}
        ORDER BY create_time DESC
    </select>

</mapper>

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fjdialect.mapper.CommentMapper">

    <select id="selectByResourceId" resultType="com.fjdialect.entity.Comment">
        SELECT * FROM comment
        WHERE resource_type = #{resourceType} AND resource_id = #{resourceId}
        ORDER BY create_time DESC
    </select>

    <select id="selectByUserId" resultType="com.fjdialect.entity.Comment">
        SELECT * FROM comment
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
    </select>

    <select id="selectByParentId" resultType="com.fjdialect.entity.Comment">
        SELECT * FROM comment
        WHERE parent_id = #{parentId}
        ORDER BY create_time ASC
    </select>

</mapper>
